# 屏幕录制权限

1.1.0 起，启动和升级不会自动执行权限重置，也不会因代码签名哈希变化主动撤销授权。

启用实时模式时，由系统管理屏幕录制授权。拒绝后不会绕过系统权限。授权异常时，可从菜单栏点击「修复屏幕录制权限…」，阅读提示并明确确认。

手动修复会停止实时模式，仅执行针对 `studio.prototype.HingeGlass.Global` 的 `tccutil reset ScreenCapture`。完成后退出并重新打开软件，再允许屏幕录制；不影响其他应用。

本机临时签名不是 Developer ID 公证签名，替换或移动不同构建仍可能影响系统授权识别；取消自动重置不能保证系统永不再次询问。请避免同时启动多个同标识副本。

休眠与锁屏恢复不应自动弹出新的权限请求：条件未满足时保留启用意图并等待。若用户已主动撤销权限，需在系统设置重新允许，或手动停止全局效果。
